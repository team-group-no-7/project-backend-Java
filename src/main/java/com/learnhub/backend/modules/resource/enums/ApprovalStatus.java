package com.learnhub.backend.modules.resource.enums;

/**
 * ApprovalStatus — Strongly typed enum representing the moderation approval status of a resource content item.
 */
public enum ApprovalStatus {
    PENDING,
    APPROVED,
    FLAGGED
}
